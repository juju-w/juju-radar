# 公开边界

此仓库只接收白名单内的网站代码和已审核公开资料。禁止提交凭据、环境配置、SSH私钥、浏览器会话、微信草稿回执、个人Zotero库、本机绝对路径以及内部部署配置。

提交前运行自定义白名单检查与 Gitleaks；CI再次检查源代码、完整Git历史和生成物。扫描报告只输出文件/规则或脱敏结果，不打印匹配值。扫描能减少风险，不能保证识别所有形式的敏感信息。

Actions固定到提交SHA，checkout不保留Git认证。PR任务只有读取权限；仅可信主分支发布任务取得contents:write。无需配置仓库Secrets，也不使用自托管Runner。

服务器无凭据拉取指定公开仓库的Release，并检查发布提交、压缩包与文件哈希。这些哈希用于完整性检查，不代替GitHub账户安全或独立签名；请启用账户双重验证，谨慎授予仓库写权限。
