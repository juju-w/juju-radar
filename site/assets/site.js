(() => {
 const dataNode=document.getElementById('catalog-data');if(!dataNode)return;
 if(new URLSearchParams(location.search).get('view')==='library'){const p=new URLSearchParams(location.search);p.delete('view');p.delete('type');location.replace('/juju-radar/papers/'+(p.size?'?'+p:''));return;}
 const library=location.pathname.replace(/\/$/,'').endsWith('/papers');
 const data=JSON.parse(dataNode.textContent), list=document.getElementById('results'), q=document.getElementById('query'),type=document.getElementById('type'),topic=document.getElementById('topic');
 const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
 const arrow='<svg class="arrow" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true"><path d="M3 10h13m-5-5 5 5-5 5"/></svg>';
 function render(){if(!list.isConnected)return;const p=new URLSearchParams(location.search);q.value=p.get('q')||'';type.value=p.get('type')||'';topic.value=p.get('topic')||'';const mode=library?'library':'daily',date=p.get('date')||data.latest,search=q.value.trim().toLocaleLowerCase();
 const filtering=!!(search||topic.value), rows=data.entries.filter(e=>(library?e.type==='paper':e.type==='news'&&(filtering||e.dates.includes(date)))&&(!topic.value||e.topics.includes(topic.value))&&(!search||[e.title_zh,e.title,e.summary,e.note,e.category,e.authors,e.url].join(' ').toLocaleLowerCase().includes(search)));
 document.getElementById('results-title').textContent=filtering?'检索结果':mode==='library'?'全部论文':date===data.latest?'最新精选':date+' 精选';document.getElementById('result-count').textContent=`${filtering||mode==='library'?'全库':date.replaceAll('-','.')} · ${rows.length} 条`;
 list.innerHTML=rows.length?rows.map(e=>`<article class="entry"><div class="meta">${esc(e.category)} · ${esc(e.date)} 收录${e.backfill?' · 补看':''}</div><h2><a href="${esc(e.path)}">${esc(e.title_zh||e.title)}</a></h2>${e.type==='paper'?`<p class="paper-original">${esc(e.title)}</p>`:''}<p>${esc(e.summary)}</p><div class="links"><button type="button" class="share-button" data-share-title="${esc(e.title_zh||e.title)}" data-share-url="https://www.asteronline.cn${esc(e.path)}"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" aria-hidden="true"><path d="M12 15V3m-4 4 4-4 4 4M5 12v8h14v-8"/></svg>分享</button><a href="${esc(e.path)}">阅读笔记 ${arrow}</a><a href="${esc(e.url)}" target="_blank" rel="noopener noreferrer">${e.type==='paper'?'论文原文':'原始资料'} ${arrow}</a></div>${e.type==='paper'?`<div class="paper-issues meta">相关期次：${e.dates.map(d=>`<a href="/juju-radar/read/${esc(d)}/">${esc(d)}</a>`).join(' · ')}</div>`:''}</article>`).join(''):'<div class="empty"><p>没有找到匹配的资料，试试更短的关键词或其他主题。</p><button id="reset">清除筛选</button></div>';
 document.querySelectorAll('nav a[data-view]').forEach(a=>a.classList.toggle('active',a.dataset.view===mode));document.querySelectorAll('.dates a').forEach(a=>a.classList.toggle('current',a.dataset.date===date&&mode==='daily'&&!filtering));document.getElementById('reset')?.addEventListener('click',()=>{history.pushState({},'',location.pathname);render()});}
 function change(){const p=new URLSearchParams(location.search);for(const [k,v]of [['q',q.value],['type',type.value],['topic',topic.value]])v?p.set(k,v):p.delete(k);history.replaceState({},'',location.pathname+(p.size?'?'+p:''));render()}
 q.addEventListener('input',change);type.addEventListener('change',change);topic.addEventListener('change',change);window.addEventListener('popstate',render);render();
})();
